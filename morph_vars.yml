---
- hosts: all
  gather_facts: true
  tasks:
    - debug:
        #morpheus: "{{ morpheus }}"
        #msg: "{{ morpheus['morpheus'] }}"
        #msg: "{{ morpheus['accountNumber'] }}"
        msg: "{{ morpheus['cloudCode'] }}"
        msg: "{{ morpheus['user']['linuxUsername'] }}"
        #msg: "{{ morpheus['instance']['container']['server']['internalIp'] }}"
        #msg: "{{ morpheus['morpheus']['container']['server']['osType'] }}"
